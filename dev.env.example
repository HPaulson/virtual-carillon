# Used for scripts/push-live.sh to test the HA integration in development
REMOTE_HOST=
HA_CONTAINER=
# Optional; otherwise scripts/reset-daily.sh discovers the running API container.
CARILLON_CONTAINER=
